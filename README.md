A simple example of using react, react router, webpack 4 and the mini-css-extract-plugin to server side render and rehydrate components on the client side.

### Running the demo

```
> yarn install
> yarn server
```